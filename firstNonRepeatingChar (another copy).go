package kata

import "strings"

func FirstNonRepeating(str string) string {
	if len(str) == 0 {
		return ""
	}

		if rep == len(str) {
			return ""
		}

		if rep == 1 {
			return string(str[i])
		}
	}

	return ""
}
